# Cube Extract — 20260730_2313

## Repo SHAs
| Repo | Branch | SHA | Dirty |
|------|--------|-----|-------|
| vrmod-x64 | main | 88cf5571a6b424b95bb18a2891704025e780aa75 | clean (after extract) |
| vrmod-module-master | linux_dev | 0cfbc8e5de25ecb64612cb506d76909e97f64cd3 | clean |

## Contracts / Laws (do not break)
1. **mat_queue_mode** = `"1"` entire VR session.
2. **Module Lua API v23** — no new exports.
3. **modules.zip** layout law.
4. **Height/Avatar**: 3D puppeteer twin (`vrmod.avatar`), not RT black-mirror.
5. **Cube Experience**: heightmenu auto-open gated by `Experience_ShouldRun()`.
6. **HUD** additive / PROPHECY.

## Progress since CUBE_EXTRACT_20260730_2304
- **88cf557** Cube Avatar menu (`cl_avatar_menu.lua`): Height · Model · Body · Bones tabs on left hand.
  - Quickmenu item **Avatar** (was Mirror); Settings posture → Open Avatar.
  - Twin via `OpenHeightCal` (id avatar); persist model/skin/bodygroups/hide flags.
  - Legacy `VRUtilOpenHeightMenu` + Experience-gated startup retained (`cl_heightadjust` thin wrapper).
  - Scrub zombie heightmenu hooks that crashed `VRUtilMenuRenderStart`.
  - `VRUtilMenuRenderStart` returns false if no RT; paint paths guard.

## Experience / avatar / UI
- Startup still respects Experience + `vrmod_heightmenu` (now labeled Avatar menu on start).
- Wrist pose via `VRUtilHandMenuPose`; always-paint avatar surface.

## Install / deploy
- install == game 67120 B; layout OK. Module unchanged.

## Known risks
- Model list may miss some workshop PMs until ListPlayerModels expanded.
- Bodygroup UI dense on 512 panel — headset verify.
- Dual AutoScale definitions (avatar_menu + heightadjust) — last load wins (same logic).

## Integration checklist
- [x] module 23 / install layout
- [x] Experience gate + puppeteer twin
- [x] Avatar customization menu
- [ ] Full fork deferred
- [ ] Headset smoke Avatar tabs + twin

## Fork recipe (document only)
```bash
cd /home/voldemar/Dev/GMod/vrmod-x64 && git branch cube/integrate-88cf557 main
cd /home/voldemar/Dev/GMod/vrmod-module-master && git branch cube/integrate-0cfbc8e linux_dev
```

## Next cube readiness
- Headset: Avatar open, switch model/body, height auto, twin alignment.

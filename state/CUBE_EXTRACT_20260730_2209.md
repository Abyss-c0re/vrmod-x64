# Cube Extract — 20260730_2209

## Repo SHAs
| Repo | Branch | SHA | Dirty |
|------|--------|-----|-------|
| vrmod-x64 | main | 0bd3386dcb053dff5381da14bbd83dc13d6271ad | clean (after this extract commit) |
| vrmod-module-master | linux_dev | adfc9aa3d9aa12b0e2d418fa304ddf4232fc59a2 | clean |

Push x64 after extract commit; module tip unchanged since 2134 extract.

## Contracts / Laws (do not break)
1. **mat_queue_mode** = `"1"` entire VR session — never change in VR or on exit.
2. **Module Lua API v23** — `GetVersion` → 23; no new exports. SS optional args only; Lua gates SS to v23+.
3. **modules.zip**: OpenVR under `bin(+linux64)` only; module under `garrysmod/lua/bin` only; never libopenvr in lua/bin.
4. **Height cal**: 3D `vrmod.avatar` with **Pescorr puppeteer IK** (not RT black-mirror). 3-point HMD+hands; FBT sixPoints upgrades waist/feet live.
5. **Cube Experience**: vision → posture; heightmenu gated by `Experience_ShouldRun()`.
6. **HUD**: additive / PROPHECY — Real must stay visible; crimson Cube UI accents.

## Progress since CUBE_EXTRACT_20260730_2134
- **c1e5ee1** Fix invalid Cube fonts: `EnsureCubeFonts` + Derma fallbacks (theme, quickmenu, weapon radial).
- **1789e74** Linux-safe faces (DejaVu Sans / Roboto) + measure-based font resolve (`vrmod.cube.Font`).
- **0bd3386** Quickmenu fallback path uses `fLabel` (no raw `"CubeLabel"` draw).
- Module: no code change (still `adfc9aa` tip / prior extracts).

## Experience / avatar / UI
- Fonts: CreateFont via preferred face → Arial → Trebuchet; fallback table to Derma*; re-ensure on InitPostEntity / resolution change.
- Quickmenu + weapon radial resolve fonts through `vrmod.cube.Font`.
- OpenHeightCal / Experience heightmenu gate unchanged.
- Crimson prophecy UI still in place.

## Install / deploy
- install DLL == game (67120 B); layout OK (OpenVR@bin, module@lua/bin).

## Known risks
- Font measure probe may still mis-detect exotic broken font names — headset verify labels.
- Puppeteer partial bone drive on exotic PMs.
- FBT auto-upgrade mid-session may flash trackers.
- Module submit stabilize still un-smoked in this loop window.
- Weapon icon material $color tint readability.

## Integration checklist
- [x] required module 23 / GetVersion 23
- [x] manage.sh layout; install≈game
- [x] Experience gate + 3D height avatar (puppeteer)
- [x] mat_queue / additive HUD
- [x] Crimson quickmenu / weapon radial
- [x] Cube fonts Linux-safe + fallbacks
- [ ] Full product fork — deferred
- [ ] Headset smoke: fonts, height twin, weapon radial

## Fork recipe (document only)
```bash
cd /home/voldemar/Dev/GMod/vrmod-x64 && git branch cube/integrate-0bd3386 main
cd /home/voldemar/Dev/GMod/vrmod-module-master && git branch cube/integrate-adfc9aa linux_dev
# Sibling Dev/GMod/; ./manage.sh; workshop 3442302711
```
Never force-push main/linux_dev.

## Next cube readiness
- Headset: confirm Cube labels render on Linux (no missing-font garbage); height twin; weapon radial.
- Fork only when packaging named cube drop.

# Cube Extract — 20260731_0051

## Repo SHAs
| Repo | Branch | SHA | Dirty |
|------|--------|-----|-------|
| vrmod-x64 | main | 3efa6167a8d35ddc2b910a69af7922268fefc45b | KDE dumps untracked only |
| vrmod-module-master | linux_dev | cf600604a12843570b67db8a3218721e98e4f8da | kde_metacam untracked |

## Laws
mat_queue=1 · API v23 · modules.zip (OpenVR under bin only) · Experience gate · 3D twin not RT

## Progress since 20260731_0024
- **3efa616** Twin frameik hardening:
  - `ReadLocalVRFrame()` — **read-only** snapshot of `g_VR.net[sid].lerpedFrame` (or ephemeral tracking fallback)
  - **Never** call `VRUtilNetUpdateLocalPly` from avatar twin (was overwriting mid-tick → real hands desync / "blocked")
  - `pcall` around Init / TransformFrame / Apply + draw path so IK errors never kill twin
  - Always park twin root in world even if IK fails
  - Stereo eye preference with desktop/debug fallback
  - `cl_frame_ik`: zero-length `targetVec` guard; safer `armStretcher` truthiness; normalize via length

## Install
67120 B install == game. Module tip unchanged (GetVersion → 23). Layout OK (no libopenvr in lua/bin).

## Experience / height
- Height cal: `cl_heightadjust` 3D path; startup auto-open skips when `Experience_ShouldRun()`.
- Onboarding: vision → posture/autoheight; sets `vrmod_heightmenu 0` on complete.

## Risks
- Headset smoke still needed (hands/fingers laterality, twin vs real hand independence)
- Requires valid clavicle/upperarm bones on PM
- armStretcher path if convar present

## Checklist
- [x] frameik integrated (20a2acc)
- [x] read-only net frame (no mid-tick overwrite)
- [x] IK safety guards
- [ ] headset smoke
- [ ] fork deferred

## Fork recipe (not created)
```bash
git -C /home/voldemar/Dev/GMod/vrmod-x64 branch cube/integrate-3efa616 main
git -C /home/voldemar/Dev/GMod/vrmod-module-master branch cube/integrate-cf60060 linux_dev
```

# Cube Extract — 20260730_2247

## Repo SHAs
| Repo | Branch | SHA | Dirty |
|------|--------|-----|-------|
| vrmod-x64 | main | b5de27410a235d6f9d0c487d15005b6b6a32fc07 | clean (after extract commit) |
| vrmod-module-master | linux_dev | 7378bec14795e6d366cd54f40d6567b84d6977d0 | clean |

## Contracts / Laws (do not break)
1. **mat_queue_mode** = `"1"` entire VR session — never change in VR or on exit.
2. **Module Lua API v23** — `GetVersion` → 23; no new exports.
3. **modules.zip**: OpenVR under `bin(+linux64)` only; module under `garrysmod/lua/bin` only.
4. **Height cal**: 3D puppeteer avatar; Experience gates heightmenu.
5. **Cube Experience**: vision → posture; heightmenu gated by `Experience_ShouldRun()`.
6. **HUD**: additive / PROPHECY. CubeUI/settings do not hide HUD or touch mat_queue.

## Progress since CUBE_EXTRACT_20260730_2227
- **690e8aa** Cube settings: rename paint callback (no shadowing `draw.*`).
- **b5de274** Left-hand Cube UI stack:
  - `cl_cube_ui.lua` — interactive 6-face left-hand cube via `VRUtilMenuOpen` (always paint).
  - Settings = cube faces (algocube path colors); desktop still Derma.
  - panel2vr: wrist default; popup/settings hand-attached (not world side-float).
  - cl_ui: clear RT **before** panel paint; keep hand scale; skip draw if no left hand.
  - quickmenu + weapon: immediate + **always paint** while open (unsummonable RT regression fixed again inside classic layout).

## Experience / avatar / UI
- Settings open on left hand as cube; `vrmod_cubeui_demo` for smoke.
- Quickmenu classic 512 grid + HudSelectionText; weapon radial seed + always paint.
- Height/Experience laws unchanged.

## Install / deploy
- install DLL == game (67120 B); layout OK. Module code unchanged this cycle.

## Known risks
- Cube settings `closeOnQuickmenu` returns false → one quickmenu press only closes cube (may feel “QM blocked”).
- Six face menus on hand may occlude / compete for laser focus — headset verify.
- Weapon menu still opens with world attach=false then keeps open-time pose (not re-parented).
- Font/exotic PM/FBT/module submit risks unchanged.

## Integration checklist
- [x] required module 23 / GetVersion 23
- [x] install layout
- [x] Experience + height avatar laws
- [x] Always-paint summonable menus
- [x] Left-hand settings / CubeUI v0
- [ ] Full product fork — deferred
- [ ] Headset smoke: settings cube, quickmenu, weapon, height

## Fork recipe (document only)
```bash
cd /home/voldemar/Dev/GMod/vrmod-x64 && git branch cube/integrate-b5de274 main
cd /home/voldemar/Dev/GMod/vrmod-module-master && git branch cube/integrate-7378bec linux_dev
```
Never force-push main/linux_dev.

## Next cube readiness
- Headset: `vrmod_cube_settings` / Settings item; quickmenu always visible; weapon radial; height twin.

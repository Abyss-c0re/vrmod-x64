# Cube Extract — 20260730_2331

## Repo SHAs
| Repo | Branch | SHA | Dirty |
|------|--------|-----|-------|
| vrmod-x64 | main | f15e6b73f973b59aef2e165cdb17e32a49820ddb | clean (after extract commit) |
| vrmod-module-master | linux_dev | 5d1cf56565b7a5bcb2667213992080e49bd00623 | clean |

## Contracts / Laws (do not break)
1. **mat_queue_mode** = `"1"` entire VR session.
2. **Module Lua API v23** — no new exports.
3. **modules.zip** layout law (OpenVR under bin(+linux64); module under garrysmod/lua/bin).
4. **Height/Avatar**: 3D puppeteer twin (`vrmod.avatar`), not RT black-mirror.
5. **Cube Experience**: heightmenu auto-open gated by `Experience_ShouldRun()`.
6. **HUD** additive / PROPHECY.

## Progress since CUBE_EXTRACT_20260730_2313
- **f15e6b7** Cube Avatar twin overhaul (`cl_avatar_editor` + `cl_avatar_menu`):
  - Twin pose = **copy live LocalPlayer bone matrices** (cl_character IK) into stand space — no home-grown MapMirror flip (was inside-out mesh).
  - Mode **facing** (default) replaces **mirror**; clone retained; legacy `mirror` remapped to facing.
  - **Laser bone pick**: point right-hand laser at twin bone, trigger hide/show; clear + persist `vrmod_avatar_hidden_bones`.
  - **SAVE TO PLAYER**: model/skin/bodygroups → cl_playermodel / skin / bodygroups + `ply.vrmod_pm`.
  - Quickmenu: defer item `func` one tick so nested `VRUtilMenuOpen` (Avatar) is not eaten by closeFunc; strip legacy Mirror menu entries.
  - Avatar open closes competing hand menus (miscmenu/heightmenu/cube_settings).

## Experience / avatar / UI
- Startup still respects Experience + `vrmod_heightmenu` via `cl_heightadjust` gate.
- Manual Avatar from quickmenu always allowed when VR active.
- Twin follow HMD+hands (+FBT waist/feet when sixPoints).

## Install / deploy
- install == game **67120 B**; layout OK. Module tip unchanged (API v23).

## Known risks
- Bone copy needs LocalPlayer SetupBones; floating-hands may force allowPlayerDraw briefly.
- Different PM vs live player maps ValveBiped names only — exotic skeletons partial.
- Laser pick radius/density on dense skeletons — headset verify.
- Dual `VRUtilOpenHeightMenu` defs (avatar_menu + heightadjust) — last load wins (both → AvatarMenu_Open).

## Integration checklist
- [x] module 23 / install layout
- [x] Experience gate + puppeteer twin
- [x] Avatar menu + live bone twin + laser hide + save PM
- [ ] Full fork deferred
- [ ] Headset smoke: facing twin, laser hide, SAVE TO PLAYER, quickmenu Avatar open

## Fork recipe (document only)
```bash
cd /home/voldemar/Dev/GMod/vrmod-x64 && git branch cube/integrate-f15e6b7 main
cd /home/voldemar/Dev/GMod/vrmod-module-master && git branch cube/integrate-5d1cf56 linux_dev
```

## Next cube readiness
- Headset: open Avatar from quickmenu, confirm twin faces user correctly, hide bones, save PM.

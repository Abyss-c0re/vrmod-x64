# Cube Extract — 20260730_2107

## Repo SHAs
| Repo | Branch | SHA | Dirty |
|------|--------|-----|-------|
| vrmod-x64 | main | fde2c2fc0a087478aae1f619ff9e5055a86aed3c | clean (post hand-cap commit) |
| vrmod-module-master | linux_dev | 7681e011c615bae507e2fb53c9fb29c1d215e3f3 | clean |

Both origin-synced after push.

## Contracts / Laws (do not break)
1. **mat_queue_mode** = `"1"` entire VR session — never change in VR or on exit.
2. **Module Lua API v23** — `GetVersion` → 23; no new exports. SS optional args on `ShareTextureBegin` only; Lua gates SS to v23+.
3. **modules.zip**: OpenVR under `install/GarrysMod/bin(+linux64)` only; module under `garrysmod/lua/bin` only; never libopenvr in lua/bin.
4. **Height cal**: 3D `vrmod.avatar` / `OpenHeightCal` — not RT black-mirror.
5. **Cube Experience**: vision → posture; heightmenu gated by `Experience_ShouldRun()`.
6. **HUD**: additive / PROPHECY — Real must stay visible.

## Progress since CUBE_EXTRACT_20260730_2057
- **x64 fce2b18**: Untie VR hands — drop stale wall `lastFree` anchors.
- **x64 (this cycle)**: Cap hand wall correction (`MAX_HAND_CORRECTION`); hull radius stays `DEFAULT_RADIUS` 2.2 (was forced ≥3).
- Module unchanged tip: `bfde40d` submit stabilize (+ extract `7681e01`).

## Experience / avatar / hands
- Hand collision: lastFree stale-drop + correction cap prevents tether-to-wall.
- Avatar height + Experience gates unchanged.
- panel2vr / cube settings / SS 1.5 contracts still in tree.

## Install / deploy
- install DLL == game (67120 B); layout law OK.

## Known risks
- Climbing/mag reload still filtered; re-test wall scrape + grip.
- Over-aggressive ClearHandWallState could briefly pop through thin walls — watch playtest.
- Old module without v23 SS path: Lua warns and ignores supersample.

## Integration checklist
- [x] required module 23 / GetVersion 23
- [x] manage.sh layout; install≈game
- [x] Experience gate + avatar height
- [x] mat_queue pin / additive HUD
- [x] Submit stabilize + SS compat
- [x] Hand untie (lastFree + radius + correction cap)
- [ ] Full product fork — deferred
- [ ] Headset smoke hands through walls/floors

## Fork recipe (document only)
```bash
cd /home/voldemar/Dev/GMod/vrmod-x64 && git branch cube/integrate-fde2c2f main
cd /home/voldemar/Dev/GMod/vrmod-module-master && git branch cube/integrate-7681e01 linux_dev
# Sibling Dev/GMod/; ./manage.sh; workshop 3442302711
```
Never force-push main/linux_dev.

## Next cube readiness
- Headset: hands free near walls; no permanent "tied" pose; submit still stable.
- Fork only when packaging named cube drop.

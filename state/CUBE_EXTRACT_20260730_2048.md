# Cube Extract — 20260730_2048

## Repo SHAs
| Repo | Branch | SHA | Dirty |
|------|--------|-----|-------|
| vrmod-x64 | main | f35608fb96fe36af23ede82a75e9f539c88132c9 | clean |
| vrmod-module-master | linux_dev | f22f69d1b859a47cc7149c8d637f6b96992bbdbe | clean |

Both origin-synced (ahead/behind 0).

## Contracts / Laws (do not break)
1. **mat_queue_mode** = `"1"` entire VR session — never change in VR or on exit.
2. **Module Lua API v23** — `GetVersion` → 23; no new Lua exports (SS optional ShareTextureBegin args only). Required 23 Linux / 21 else; addon 200.
3. **modules.zip**: OpenVR only under `install/GarrysMod/bin(+linux64)`; module only under `garrysmod/lua/bin`; never libopenvr in lua/bin.
4. **Height cal**: 3D `vrmod.avatar` / `OpenHeightCal` — not RT black-mirror.
5. **Cube Experience**: vision → posture; heightmenu gated by `Experience_ShouldRun()`.
6. **HUD**: additive / PROPHECY — Real must stay visible.

## Progress since CUBE_EXTRACT_20260730_2039
- **panel2vr** (was web2vr WIP): Derma/VGUI → VR surfaces framework (`cl_panel2vr.lua`); dermapopups delegated.
- **Glorious Crimson Cube settings** (`cl_cube_settings.lua`) via panel2vr native manifest.
- **Workshop watchlist** (`docs/CUBE_WATCHLIST.md`): swap eyes cvar + drop blocked FOV cvar path (`6bca3ff`).
- Module unchanged feature tip: `79d16ed` crisp 4096 path (extract tip was `f22f69d`).

## Experience / avatar / HUD / UI
- Height + FBT still share `vrmod.avatar`.
- Settings entry via `vrmod.panel2vr.OpenSettings` / cube buttons.
- Supersample default 1.5 + 4096 clamp still contract with module.
- `vrmod_swap_eyes` available if stereo inverted (watchlist item).

## Install / deploy
- install DLL matches game (66856 B, 4096 crisp build).
- Layout law OK (no openvr in lua/bin).

## Known risks
- panel2vr MakePopup intercept may fight exotic addons — watch dual menus.
- Eye swap mis-set confuses stereo SoT — default must stay correct.
- Do not reintroduce blocked engine FOV cvars that freeze/break VR.
- Module+addon SS size mismatch if old module binary left in game.

## Integration checklist
- [x] required module 23 / GetVersion 23
- [x] manage.sh layout; install≈game
- [x] Experience gate + avatar height
- [x] Additive HUD / mat_queue pin
- [x] SS 1.5 / 4096 contract
- [x] panel2vr + cube settings landed (clean tree)
- [ ] Full product fork — deferred
- [ ] In-headset smoke panel2vr settings + eye swap + SS

## Fork recipe (document only)
```bash
cd /home/voldemar/Dev/GMod/vrmod-x64 && git branch cube/integrate-f35608f main
cd /home/voldemar/Dev/GMod/vrmod-module-master && git branch cube/integrate-f22f69d linux_dev
# Sibling Dev/GMod/; ./manage.sh deploy; workshop 3442302711
```
Never force-push main/linux_dev.

## Next cube readiness
- Headset: open Cube settings via panel2vr; verify laser; no black slabs; experience still first-run safe.
- Optional named fork when packaging Desktop modules.zip + workshop drop.

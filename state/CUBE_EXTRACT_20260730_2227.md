# Cube Extract — 20260730_2227

## Repo SHAs
| Repo | Branch | SHA | Dirty |
|------|--------|-----|-------|
| vrmod-x64 | main | 6ac9a28c64748128ea995934ea372cb7b6d64e4c | clean |
| vrmod-module-master | linux_dev | 326e9133a55136ef0cd8043a3b181cdeacfee3f9 | clean |

Both origin-synced (x64 tip = menu fix; module tip still extract-only from 2209).

## Contracts / Laws (do not break)
1. **mat_queue_mode** = `"1"` entire VR session — never change in VR or on exit.
2. **Module Lua API v23** — `GetVersion` → 23; no new exports.
3. **modules.zip**: OpenVR under `bin(+linux64)` only; module under `garrysmod/lua/bin` only; never libopenvr in lua/bin.
4. **Height cal**: 3D `vrmod.avatar` puppeteer IK (not RT black-mirror); Experience gates heightmenu.
5. **Cube Experience**: vision → posture; heightmenu gated by `Experience_ShouldRun()`.
6. **HUD**: additive / PROPHECY — Real must stay visible; crimson Cube UI accents.

## Progress since CUBE_EXTRACT_20260730_2209
- **6ac9a28** Fix unsummonable menus: always paint while open; engine fonts only (Derma*).
  - Weapon menu was transparent until laser focus (empty RT).
  - Quickmenu: pcall paint path, immediate paint on open, force `open=false` on close/fail/exit, nil-safe slots/cursor.
  - Dropped runtime dependence on unregistered Cube* font names for summonable menus (hard Derma* only).
- Font foundation still present in `cl_cube_theme.lua` for other UI; menus no longer require it to appear.
- Module: no code change.

## Experience / avatar / UI
- Quickmenu + weapon radial: paint every open frame; pcall-wrapped open/paint; VR exit cleans hooks.
- `vrmod_quickmenu` concommand for debug re-summon.
- Height/Experience laws unchanged.

## Install / deploy
- install DLL == game (67120 B); layout OK.

## Known risks
- Engine-only fonts = less custom look on menus until Cube fonts proven safe in-headset.
- Headset smoke still needed for quickmenu + weapon radial laser select.
- Puppeteer exotic PM / FBT flash / module submit stabilize (unchanged).

## Integration checklist
- [x] required module 23 / GetVersion 23
- [x] manage.sh layout; install≈game
- [x] Experience gate + 3D height avatar
- [x] mat_queue / additive HUD
- [x] Menu always-paint + engine fonts (unsummonable fix)
- [ ] Full product fork — deferred
- [ ] Headset smoke: menus + height twin

## Fork recipe (document only)
```bash
cd /home/voldemar/Dev/GMod/vrmod-x64 && git branch cube/integrate-6ac9a28 main
cd /home/voldemar/Dev/GMod/vrmod-module-master && git branch cube/integrate-326e913 linux_dev
# Sibling Dev/GMod/; ./manage.sh; workshop 3442302711
```
Never force-push main/linux_dev.

## Next cube readiness
- Headset: summon quickmenu + weapon menu without laser-first blank RT; confirm close/exit cleanup.
- Fork only for named cube drop.

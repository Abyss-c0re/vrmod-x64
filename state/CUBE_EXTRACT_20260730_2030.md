# Cube Extract — 20260730_2030

## Repo SHAs
| Repo | Branch | SHA | Dirty |
|------|--------|-----|-------|
| vrmod-x64 | main | 22b48be0acf15c3fd0314363f0e76f605a6a2bad | clean |
| vrmod-module-master | linux_dev | 785bfdb8fac9dbdd420cfac88576534eb3b7a843 | clean |

Both tracking origin; ahead/behind 0 after fetch.

## Contracts / Laws (do not break)
1. **mat_queue_mode** pinned to `"1"` for entire VR session (`cl_vrmod.lua`); never change in VR or on exit.
2. **Module Lua API** stays at **v23** (`GetVersion` → 23). No casual new Lua exports. Addon requires module 23 on Linux / 21 elsewhere; `latestModuleVersion = 23`. Addon version = 200.
3. **modules.zip layout** (Desktop reference):
   - OpenVR only: `install/GarrysMod/bin/{libopenvr_api.so,openvr_api.dll,openvr_license,linux64/libopenvr_api.so}`
   - Module only: `install/GarrysMod/garrysmod/lua/bin/gmcl_vrmod_linux64.dll`
   - **Never** `libopenvr_api.so` under `lua/bin` (manage.sh rejects).
4. **Height cal** uses live **3D player avatar** via unified `vrmod.avatar` (`cl_avatar_editor.lua` ClientsideModel) — not broken RT mirror.
5. **Cube Experience** (`cl_onboarding.lua`): first-run Welcome → Vision → Posture → Complete. Heightmenu auto-open **gated** by `Experience_ShouldRun()`.
6. **HUD**: additive UnlitGeneric + alpha RT — overlay must not occlude the Real (`PROPHECY_REAL_GMOD.md`).

## Experience / avatar / HUD notes
- **NEW (22b48be):** Height + FBT calibration share `vrmod.avatar` SoT (mirror/clone/world modes; HMD/hands/waist/feet bone tracking).
- Height menu: `vrmod.avatar.OpenHeightCal("heightmenu")`; FBT: `OpenFBTCal()`.
- Experience still sets `vrmod_heightmenu 0` on complete; startup heightmenu skips while experience should run.
- Module: quality ladder + dual-texture submit; **no new Lua exports** (still d883566 / extract 785bfdb).

## Install / deploy readiness
- install tree present; module DLL matches game bin mtime/size.
- OpenVR not in lua/bin under install.
- Package via `./manage.sh zip` → `$HOME/Desktop/modules.zip`.

## Known risks
- Workshop dual-load if both GMA and loose addon present.
- Linux-only module branch `linux_dev` vs addon `main` — contract glue = required 23.
- Avatar util must load before height/FBT open (loader walks `utils/` — OK).
- Do not bump API past 23 without coordinated release.

## Integration checklist
- [x] Addon+module version contract (required 23 / GetVersion 23)
- [x] manage.sh layout law
- [x] Experience first-run + heightmenu gate
- [x] Unified 3D avatar height/FBT
- [x] Additive HUD / PROPHECY
- [x] mat_queue pin
- [ ] Full product fork — deferred (recipe below)

## Fork recipe (document only)
```bash
cd /home/voldemar/Dev/GMod/vrmod-x64 && git branch cube/integrate-22b48be main
cd /home/voldemar/Dev/GMod/vrmod-module-master && git branch cube/integrate-785bfdb linux_dev
# Sibling dirs under Dev/GMod/; deploy module manage.sh; addon workshop 3442302711
```
Never force-push main/linux_dev.

## Progress since prior extract (20260730_2021)
- **x64 22b48be** Unify height and FBT avatars in one `vrmod.avatar` editor util (+ docs/AVATAR_EDITOR.md)
- Prior: Cube Experience, mat_queue pin, stereo SoT; module quality ladder + modules.zip layout

## Next cube readiness
- In-headset: height cal + FBT cal both show same PM twin; experience still not fighting heightmenu.
- Optional named cube drop: cut `cube/integrate-*` only when packaging.

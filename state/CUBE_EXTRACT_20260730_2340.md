# Cube Extract — 20260730_2340

## Repo SHAs
| Repo | Branch | SHA | Dirty |
|------|--------|-----|-------|
| vrmod-x64 | main | aef06eaf9d09297c8b04ec49ef335c624c8d65d3 | clean (after extract) |
| vrmod-module-master | linux_dev | 483a83492d4f72f779daf88e6bcc39a5e12fa974 | clean |

## Contracts / Laws (do not break)
1. **mat_queue_mode** = `"1"` entire VR session.
2. **Module Lua API v23** — no new exports.
3. **modules.zip** layout law.
4. **Height/Avatar**: 3D puppeteer twin (`vrmod.avatar`), not RT black-mirror.
5. **Cube Experience**: heightmenu auto-open gated by `Experience_ShouldRun()`.
6. **HUD** additive / PROPHECY.

## Progress since CUBE_EXTRACT_20260730_2331
- **aef06ea** Avatar true mirror + tracking harden:
  - `MapMirror`: sagittal L/R flip + re-handed basis (`AngleFromBasis`) so bone matrices are not inside-out.
  - Twin bone copy routes through `_map` (mirror/facing vs clone); frame uses `g_VR.characterYaw` + HMD feet.
  - UI: FACING button labeled **MIRROR** (mode still `facing` internally; legacy mirror→facing).
  - `sh_math`: `AddVec` / `MulVec`; `LengthSqr` nil-safe.
  - Pose max-delta clamp: pcall + fallbacks so missing utils never kill a tracking frame. **mat_queue untouched.**

## Experience / avatar / UI
- Experience gate + heightmenu auto-open unchanged.
- Laser hide / SAVE TO PLAYER from prior extract still present.

## Install / deploy
- install == game **67120 B**; layout OK. Module tip unchanged (API v23).

## Known risks
- Mirror chirality: verify in-headset hands map left↔right and mesh not inverted.
- Different-PM bone name map still partial.
- characterYaw dependency if character system off mid-frame.

## Integration checklist
- [x] module 23 / install layout
- [x] Experience gate + puppeteer twin
- [x] Live bone twin + true mirror + laser + save + clamp harden
- [ ] Full fork deferred
- [ ] Headset smoke: MIRROR laterality + mesh; pose clamp no spam

## Fork recipe (document only)
```bash
cd /home/voldemar/Dev/GMod/vrmod-x64 && git branch cube/integrate-aef06ea main
cd /home/voldemar/Dev/GMod/vrmod-module-master && git branch cube/integrate-483a834 linux_dev
```

## Next cube readiness
- Headset verify true mirror laterality/mesh; clone mode contrast; tracking stability.

# Algocube prototypes (not loaded)

These `.lua` files are **not** under `lua/vrmod/` and are **not** included by VRMod’s loader.

To experiment later, copy or symlink into `lua/vrmod/utils/` deliberately — never by default.
